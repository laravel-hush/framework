<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Page settings
    |--------------------------------------------------------------------------
    */

    'default' => [
        'class' => null,
        'title' => '{{ singular }}',
        'breadcrumbs' => [
            '{{ plural }}' => 'page:admin.{{ plural }}',
            'edit' => null,
        ],
        'permission' => ['admin:{{ plural }}_add', 'admin:{{ plural }}_edit'],

        'closure' => function () {
            // should be returned ['model']
        },


        'blocks' => [
            [
                'class' => 'col-12',

                'title' => [
                    'text' => '{{ singular }}',
                    'buttons' => [
                        [
                            'form' => '{{ plural }}',
                            'text' => 'save',
                            'icon' => 'save',
                            'class' => 'btn-primary'
                        ],
                    ],
                ],

                /*
                |--------------------------------------------------------------
                | Block content
                |--------------------------------------------------------------
                */

                'content' => [
                    'id' => '{{ plural }}',
                    'type' => 'form',
                    'link' => 'action:save',
                    'inputs' => [
                        [
                            'type' => 'hidden',
                            'name' => 'id'
                        ],

                        // another inputs
                    ]
                ]

            ]
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Page actions
    |--------------------------------------------------------------------------
    */

    'post' => [

        'save' => [
            'permission' => ['admin:{{ plural }}_add', 'admin:{{ plural }}_edit'],
            'rules' => function () {
                return [
                    // validation rules
                ];
            },
            'closure' => function () {
                // some actions
                // if nothing returned here than will be returned default response
            }
        ]

    ],
];
